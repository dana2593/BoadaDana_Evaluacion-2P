<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BoadaDana_Evaluación_2P.JokesPage"
             Title="Chistes Divertidos"
             BackgroundColor="#ECEFF4">

    <AbsoluteLayout>
        <ScrollView 
            AbsoluteLayout.LayoutBounds="0,0,1,0.85" 
            AbsoluteLayout.LayoutFlags="All"
            Padding="20">

            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Frame BackgroundColor="White" 
                       CornerRadius="15" 
                       HasShadow="True"
                       Padding="20"
                       Margin="10">

                    <Label 
                        x:Name="jokeLabel"
                        Text="Cargando chiste divertido..."
                        FontSize="18"
                        LineHeight="1.5"
                        HorizontalTextAlignment="Center"
                        VerticalTextAlignment="Center"
                        TextColor="#2E3440"
                        MinimumHeightRequest="150" />
                </Frame>
            </VerticalStackLayout>
        </ScrollView>

        <Button 
            x:Name="newJokeButton"
            Text="🎭 Otro chiste"
            BackgroundColor="#BF616A"
            TextColor="White"
            FontSize="16"
            FontAttributes="Bold"
            CornerRadius="25"
            Clicked="OnNewJokeClicked"
            AbsoluteLayout.LayoutBounds="0.5,0.95,0.8,50" 
            AbsoluteLayout.LayoutFlags="PositionProportional,WidthProportional" />
    </AbsoluteLayout>
</ContentPage>